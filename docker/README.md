# Docker

This directory contains container orchestration for local development.

Use:
- `docker compose -f docker/docker-compose.yml up --build`
- `docker compose -f docker/docker-compose.yml down --volumes`
